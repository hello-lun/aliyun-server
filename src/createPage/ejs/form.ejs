
<el-form :inline="true"> -%>
    <% for(var i = 0; i < data.length; i++){ %>
        <el-form-item label="<%= data[i].title %>">
            <% var pathName = data[i].type + '.ejs'; %>
            <%- include(pathName, {option: data[i]}); %>
        </el-form-item>
    <% } -%>
</el-form>